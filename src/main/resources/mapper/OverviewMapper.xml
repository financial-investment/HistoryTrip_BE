<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.history.mapper.OverviewMapper">
    <select id="countCoreTables" resultType="com.ssafy.history.dto.TableCountDto">
        SELECT 'regions' AS table_name, COUNT(*) AS row_count FROM regions
        UNION ALL SELECT 'historical_places', COUNT(*) FROM historical_places
        UNION ALL SELECT 'history_tags', COUNT(*) FROM history_tags
        UNION ALL SELECT 'place_tags', COUNT(*) FROM place_tags
        UNION ALL SELECT 'quizzes', COUNT(*) FROM quizzes
        UNION ALL SELECT 'quiz_tags', COUNT(*) FROM quiz_tags
        UNION ALL SELECT 'region_history_tags', COUNT(*) FROM region_history_tags
        UNION ALL SELECT 'hotplaces', COUNT(*) FROM hotplaces
        UNION ALL SELECT 'boards', COUNT(*) FROM boards
        UNION ALL SELECT 'notices', COUNT(*) FROM notices
    </select>
</mapper>
